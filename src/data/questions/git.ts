import type { Question } from '@/types/quiz';

export const gitQuestions: Question[] = [
  {
    id: 'git-1',
    category: 'git',
    question: 'Что в действительности создает команда `git init` под капотом в локальной директории?',
    options: [
      'Скрытую папку .git, содержащую структуру каталогов (objects, refs), файл HEAD, конфигурационный файл и индекс',
      'Зашифрованную резервную копию всех текущих файлов проекта во временной папке ОС',
      'Скрытый файл .gitconfig в домашней директории текущего пользователя',
      'Удаленную ссылку в глобальном реестре Git, связывающую проект с сервером по умолчанию'
    ],
    correctAnswer: 0,
    explanation: 'Инициализация репозитория через `git init` создает директорию `.git`, которая содержит: `objects/` (объектную базу blob, tree, commit, tag), `refs/` (указатели на ветки и теги), текстовый файл `HEAD` (указывающий на текущую ветку) и локальный файл `config`.',
  },
  {
    id: 'git-2',
    category: 'git',
    question: 'Что произойдет, если попытаться выполнить команду `git clone <url> .` в директории, которая уже содержит какие-то файлы?',
    options: [
      'Git перезапишет все локальные конфликтующие файлы без предупреждения',
      'Команда завершится ошибкой, так как Git разрешает клонировать только в пустую директорию',
      'Файлы в директории автоматически добавятся в стейджинг удаленного репозитория',
      'Git создаст вложенную папку с именем репозитория внутри текущей директории'
    ],
    correctAnswer: 1,
    explanation: 'Клонирование в существующую директорию через `git clone <url> .` возможно только в том случае, если эта директория абсолютно пуста. Если в ней есть хоть один файл, Git выдаст ошибку: `fatal: destination path \'.\' already exists and is not an empty directory.`',
  },
  {
    id: 'git-3',
    category: 'git',
    question: 'В чем основное отличие команды `git add -p` от команды `git add .`?',
    options: [
      'git add -p добавляет только файлы, которые начинаются на букву p',
      'git add -p автоматически отправляет изменения на удаленный сервер в фоновом режиме',
      'git add -p позволяет интерактивно просмотреть каждое изменение (hunk) в файлах и выбрать, добавлять ли его в индекс',
      'git add -p добавляет в индекс только новые (untracked) файлы, игнорируя измененные'
    ],
    correctAnswer: 2,
    explanation: 'Режим `git add -p` (или `--patch`) запускает интерактивный режим, разбивая изменения в файлах на блоки (hunks). Разработчик может одобрить (y), отклонить (n), разбить блок (s) или отредактировать его, что позволяет формировать точечные атомарные коммиты.',
  },
  {
    id: 'git-4',
    category: 'git',
    question: 'Из каких основных компонентов состоит создаваемый Git объект коммита (commit object)?',
    options: [
      'Только из SHA-1 хэша коммита и тела коммита',
      'Из списка измененных файлов, их полных путей и разницы в формате unified diff',
      'Из имени текущей ветки, версии Git и времени последней сетевой синхронизации',
      'Из ссылки на дерево файлов (tree), ссылок на родительские коммиты (parents), автора, коммитера, даты и сообщения коммита'
    ],
    correctAnswer: 3,
    explanation: 'Объект коммита в Git — это текстовый файл, содержащий: SHA-1 ссылку на объект дерева (`tree`), представляющего состояние корня проекта; ссылки на родительские коммиты `parent` (ноль для первого коммита, один для обычного, два и более для мерж-коммитов); метаданные автора (`author`) и коммитера (`committer`) с временными метками; и текстовое сообщение коммита.',
  },
  {
    id: 'git-5',
    category: 'git',
    question: 'Как Git отслеживает права доступа к файлам (file permissions) в своей базе данных?',
    options: [
      'Git сохраняет только флаг исполняемого файла (executable bit: 100755 или 100644), игнорируя остальные права записи/чтения для групп и пользователей',
      'Git полностью копирует стандартную POSIX таблицу прав доступа для каждого файла и папки',
      'Git игнорирует любые изменения прав доступа на всех операционных системах',
      'Git записывает права доступа только в том случае, если включена глобальная опция `git config core.sharedRepository`'
    ],
    correctAnswer: 0,
    explanation: 'Для простоты и кроссплатформенности Git хранит только тип файла и бит исполняемости в метаданных дерева (tree). Внутренние режимы (modes) для файлов ограничены значениями `100644` (обычный файл) и `100755` (исполняемый файл). Любые изменения прав на чтение/запись для владельца/группы самой базой Git не отслеживаются.',
  },
  {
    id: 'git-6',
    category: 'git',
    question: 'Какая комбинация флагов команды `git log` выведет компактную историю в виде текстового графа ветвлений с сокращенными хэшами коммитов?',
    options: [
      'git log --pretty=raw --abbrev-commit',
      'git log --graph --oneline --decorate',
      'git log --branches --simplify-by-decoration',
      'git log --stat --summary'
    ],
    correctAnswer: 1,
    explanation: 'Команда `git log --graph --oneline --decorate` строит лаконичный интерактивный граф с использованием ASCII-символов (`--graph`), сокращает хэши до 7 символов и выводит в одну строку (`--oneline`), а также показывает названия веток, тегов и HEAD (`--decorate`).',
  },
  {
    id: 'git-7',
    category: 'git',
    question: 'Какая команда покажет разницу между изменениями, уже добавленными в индекс (staged), и последним коммитом?',
    options: [
      'git diff',
      'git diff --unstaged',
      'git diff --staged (или --cached)',
      'git diff HEAD..WORKING'
    ],
    correctAnswer: 2,
    explanation: 'Команда `git diff` без параметров сравнивает рабочую директорию с индексом (unstaged изменения). Чтобы сравнить индекс с последним коммитом (staged изменения), нужно использовать флаг `--staged` или `--cached`.',
  },
  {
    id: 'git-8',
    category: 'git',
    question: 'Вы хотите переименовать локальную ветку `feature-old` в `feature-new` и обновить ее на удаленном сервере. Какая последовательность команд является наиболее корректной?',
    options: [
      'git branch -d feature-old && git branch -a feature-new',
      'git checkout feature-old && git branch --rename feature-new && git push origin feature-new',
      'git checkout feature-old; git branch -m feature-new; git push origin :feature-old && git push -u origin feature-new',
      'git branch -m feature-old feature-new && git push origin --all'
    ],
    correctAnswer: 2,
    explanation: 'Для переименования текущей ветки используется `git branch -m feature-new`. Чтобы обновить это на сервере, нужно удалить старую ветку из remote (команда `git push origin :feature-old` или `git push origin --delete feature-old`) и отправить новую ветку с установкой upstream-связи (`git push -u origin feature-new`).',
  },
  {
    id: 'git-9',
    category: 'git',
    question: 'В чем заключается разница между новыми командами `git switch` и `git restore`, введенными в Git 2.23, по сравнению со старой `git checkout`?',
    options: [
      'git switch предназначена строго для переключения веток и создания новых, а git restore — для отката изменений в файлах; git checkout совмещала обе эти роли',
      'git switch работает только с удаленными репозиториями, а git restore — только с локальными',
      'git restore удаляет файлы навсегда без возможности восстановления, а git switch переключает режимы конфигурации',
      'git switch была добавлена для совместимости с SVN, а git restore восстанавливает поврежденную базу данных .git'
    ],
    correctAnswer: 0,
    explanation: 'До версии 2.23 команда `git checkout` выполняла две семантически разные задачи: переключение веток и восстановление/откат файлов. Для разделения этих обязанностей и упрощения UX были добавлены `git switch` (управление ветками) и `git restore` (восстановление файлов из индекса или коммита).',
  },
  {
    id: 'git-10',
    category: 'git',
    question: 'Что означает состояние detached HEAD в Git и каков наиболее безопасный способ сохранить изменения, сделанные в этом состоянии?',
    options: [
      'HEAD указывает на несуществующую удаленную ветку; нужно сделать git pull origin main',
      'Указатель HEAD ссылается непосредственно на конкретный коммит, а не на именованную ветку. Чтобы сохранить сделанные коммиты, нужно создать новую ветку прямо сейчас с помощью `git switch -c <branch-name>`',
      'Локальный репозиторий поврежден из-за прерывания процесса слияния; нужно удалить папку .git и инициализировать проект заново',
      'Ветка была удалена другим разработчиком; нужно выполнить git merge --abort'
    ],
    correctAnswer: 1,
    explanation: 'В состоянии detached HEAD указатель `HEAD` смотрит прямо на SHA-1 коммита. Любые созданные здесь коммиты не принадлежат ни одной ветке и при переключении на другую ветку могут быть со временем удалены сборщиком мусора (garbage collector). Чтобы сохранить их, нужно создать ветку на текущем месте: `git switch -c new-feature-branch`.',
  },
  {
    id: 'git-11',
    category: 'git',
    question: 'В каком случае Git выполняет Fast-forward слияние (merge) по умолчанию?',
    options: [
      'Когда сливаемые ветки содержат абсолютно одинаковые изменения',
      'Когда в целевой ветке не появилось новых коммитов с момента расхождения с веткой, которую мы вливаем, и указатель целевой ветки можно просто передвинуть вперед',
      'Когда в коммитах отсутствуют текстовые конфликты, и Git может объединить их автоматически без создания коммита',
      'Когда при вызове слияния был передан специальный флаг `git merge --fast-forward-only`'
    ],
    correctAnswer: 1,
    explanation: 'Fast-forward слияние происходит, если история целевой ветки не разошлась с историей вливаемой ветки (т.е. в целевой ветке со времени ветвления не было новых коммитов). Git просто перемещает указатель целевой ветки вперед на последний коммит вливаемой ветки, не создавая при этом специальный коммит слияния (merge commit).',
  },
  {
    id: 'git-12',
    category: 'git',
    question: 'Какое из утверждений точнее всего описывает разницу между `git merge` и `git rebase`?',
    options: [
      'git merge перезаписывает историю проекта, а git rebase сохраняет точную хронологию событий',
      'git merge переносит изменения по одному коммиту за раз, а git rebase объединяет их в один большой коммит автоматически',
      'git merge создает новый коммит слияния, сохраняя оригинальную хронологическую историю веток, тогда как git rebase переносит базу текущей ветки на другой коммит, переписывая историю коммитов и делая ее линейной',
      'git merge работает только локально, а git rebase синхронизирует изменения с удаленным сервером'
    ],
    correctAnswer: 2,
    explanation: '`git merge` соединяет две ветки с помощью специального merge-коммита, сохраняя историю коммитов в том виде, в котором она была создана. `git rebase` берет коммиты текущей ветки, временно откладывает их, обновляет базу ветки до указанного коммита, а затем применяет отложенные коммиты заново (создавая новые объекты коммитов с новыми SHA), что делает историю линейной.',
  },
  {
    id: 'git-13',
    category: 'git',
    question: 'При интерактивном ребейзе (`git rebase -i`), в чем заключается разница между действиями `squash` и `fixup`?',
    options: [
      'squash сохраняет изменения, но удаляет коммит из истории, а fixup оставляет коммит пустым',
      'Оба действия объединяют коммит с предыдущим, но squash позволяет отредактировать и объединить сообщения коммитов, а fixup просто использует сообщение предыдущего коммита, отбрасывая текущее',
      'fixup применяется только к конфликтующим коммитам, в то время как squash работает с любыми коммитами без конфликтов',
      'squash объединяет коммиты сверху вниз, а fixup — снизу вверх по списку'
    ],
    correctAnswer: 1,
    explanation: 'При `git rebase -i` действия `squash` (s) и `fixup` (f) сливают коммит с предшествующим ему по списку (вышестоящим). Различие в том, что `squash` открывает текстовый редактор для объединения и редактирования сообщений обоих коммитов, а `fixup` молча отбрасывает сообщение текущего коммита и оставляет только сообщение предыдущего.',
  },
  {
    id: 'git-14',
    category: 'git',
    question: 'Что произойдет, если в списке инструкций интерактивного ребейза (`git rebase -i`) заменить команду `pick` перед коммитом на `edit`?',
    options: [
      'Процесс ребейза остановится на этом коммите, позволяя вам внести изменения в код, добавить их в индекс и изменить коммит с помощью `git commit --amend`',
      'Git откроет интерактивный редактор для изменения только сообщения коммита, не останавливая выполнение ребейза',
      'Этот коммит будет удален из истории, а его изменения будут перемещены в рабочую директорию как неотслеживаемые',
      'Git запустит автоматическое тестирование этого коммита и перейдет к следующему в случае успеха'
    ],
    correctAnswer: 0,
    explanation: 'Команда `edit` приостанавливает выполнение интерактивного ребейза непосредственно после применения выбранного коммита. Разработчик может протестировать код, внести исправления, добавить их в индекс (`git add`), изменить коммит (`git commit --amend`), а затем продолжить процесс ребейза командой `git rebase --continue`.',
  },
  {
    id: 'git-15',
    category: 'git',
    question: 'Для чего используется команда `git cherry-pick` и как работает ее опция `-n` (или `--no-commit`)?',
    options: [
      'Она удаляет выбранный коммит из текущей ветки и переносит его в ветку по умолчанию; опция -n запрещает сетевые запросы',
      'Она применяет изменения указанного коммита в текущую ветку; опция -n применяет изменения только в рабочую директорию и индекс, не создавая новый коммит автоматически',
      'Она сравнивает две ветки и выводит список уникальных коммитов; опция -n скрывает имена авторов изменений',
      'Она используется для отката изменений выбранного коммита; опция -n отменяет создание логов'
    ],
    correctAnswer: 1,
    explanation: '`git cherry-pick <commit-hash>` берет изменения конкретного коммита и применяет их к текущей ветке в виде нового коммита. Если добавить флаг `-n` / `--no-commit`, Git применит изменения к рабочему дереву и индексу, но не будет создавать коммит автоматически, позволяя разработчику скорректировать файлы или объединить несколько cherry-pick в один коммит.',
  },
  {
    id: 'git-16',
    category: 'git',
    question: 'В чем разница между командами `git stash pop` и `git stash apply`?',
    options: [
      'git stash pop удаляет верхний элемент из стека stash после его успешного применения, в то время как git stash apply оставляет его в списке сохраненных состояний',
      'git stash pop работает только с отслеживаемыми файлами, а git stash apply — со всеми, включая .gitignore',
      'git stash apply автоматически создает мерж-коммит в случае конфликтов, а git stash pop аварийно завершает работу',
      'git stash pop восстанавливает файлы в рабочую директорию, а git stash apply — сразу в коммит на удаленном сервере'
    ],
    correctAnswer: 0,
    explanation: 'Обе команды восстанавливают последние сохраненные изменения из стека stash. Разница в том, что `git stash pop` при успешном применении (без конфликтов) автоматически удаляет запись из списка stash (эквивалентно `git stash apply && git stash drop`), а `git stash apply` сохраняет запись в стеке `git stash list`.',
  },
  {
    id: 'git-17',
    category: 'git',
    question: 'Как сохранить в stash не только измененные файлы, находящиеся под отслеживанием Git, но также и абсолютно новые, неотслеживаемые файлы (untracked files)?',
    options: [
      'git stash --all-files',
      'git stash -u (или --include-untracked)',
      'git stash --force-untracked',
      'Использовать команду git stash save, так как она по умолчанию сохраняет все untracked файлы'
    ],
    correctAnswer: 1,
    explanation: 'По умолчанию `git stash` сохраняет только изменения в отслеживаемых (tracked) файлах. Неотслеживаемые файлы игнорируются. Чтобы включить их в stash, необходимо передать флаг `-u` или `--include-untracked` (флаг `-a` или `--all` включает также и проигнорированные в `.gitignore` файлы).',
  },
  {
    id: 'git-18',
    category: 'git',
    question: 'Как изменятся рабочая область (working directory), индекс (index/staging) и указатель ветки (HEAD) после выполнения команды `git reset --mixed HEAD~1`?',
    options: [
      'Указатель ветки сдвинется на один коммит назад; индекс и рабочая область останутся неизменными (все изменения сохранены в стейджинге)',
      'Указатель ветки сдвинется на один коммит назад; индекс очистится (изменения будут убраны из стейджинга), а рабочая директория останется нетронутой (изменения останутся в файлах)',
      'Указатель ветки сдвинется назад, а рабочая область и индекс будут полностью стерты и приведены к состоянию коммита HEAD~1 (изменения безвозвратно утеряны)',
      'Команда выполнит откат только в рабочей области, не сдвигая указатель ветки HEAD'
    ],
    correctAnswer: 1,
    explanation: 'Флаг `--mixed` является поведением по умолчанию для `git reset`. Указатель HEAD перемещается на целевой коммит, индекс (staging area) сбрасывается под состояние этого коммита (изменения убираются из индекса), но рабочая директория (файлы на диске) не изменяется, оставляя все изменения в виде "unstaged".',
  },
  {
    id: 'git-19',
    category: 'git',
    question: 'В каких случаях предпочтительно использовать `git revert`, а не `git reset`, и в чем ключевое отличие revert от reset?',
    options: [
      'git revert используется для отката изменений, которые уже были отправлены в общий удаленный репозиторий, так как она создает новый коммит, отменяющий изменения старого коммита, не переписывая историю',
      'git revert используется только для локального отката файлов, так как она не создает новых коммитов в отличие от git reset',
      'git revert физически удаляет коммиты из базы данных Git, высвобождая место на диске',
      'git revert позволяет отменить изменения только в нетекстовых файлах (картинки, архивы)'
    ],
    correctAnswer: 0,
    explanation: '`git reset` переписывает историю ветки, перемещая ее указатель назад. Если эти коммиты уже были отправлены (pushed) в общий удаленный репозиторий, использование `git reset` приведет к несовпадению историй у коллег и конфликтам при пуше. `git revert` — это безопасный способ отката в публичных ветках, так как она создает новый коммит, содержащий обратные изменения, не меняя существующую историю.',
  },
  {
    id: 'git-20',
    category: 'git',
    question: 'Что такое `git reflog` и как с его помощью восстановить ветку, которая была удалена локально через `git branch -D`?',
    options: [
      'Это лог сетевых запросов к серверу; восстановить ветку можно только путем повторного клонирования',
      'Это резервная копия всех конфигурационных файлов репозитория; восстановить ветку можно через git config --restore',
      'Это журнал изменений указателя HEAD и локальных веток. Нужно найти в выводе reflog запись переключения на удаленную ветку перед ее удалением, скопировать хэш коммита и выполнить `git checkout -b <branch-name> <commit-hash>`',
      'Это база данных удаленных файлов; восстановить ветку можно через git checkout --force'
    ],
    correctAnswer: 2,
    explanation: '`git reflog` записывает каждое перемещение указателя `HEAD` локально (переключение веток, коммиты, ребейзы, сбросы). Записи хранятся обычно 90 дней. Даже если ветка была удалена локально через `git branch -D`, коммиты все еще физически существуют в базе. Найдя в выводе `git reflog` хэш последнего коммита удаленной ветки, можно воссоздать ее командой `git checkout -b <branch-name> <commit-hash>`.',
  },
  {
    id: 'git-21',
    category: 'git',
    question: 'Что в действительности делает команда `git pull` по умолчанию под капотом?',
    options: [
      'Она скачивает файлы напрямую по протоколу HTTP и перезаписывает рабочую область без учета веток',
      'Она выполняет последовательно команды `git fetch` (загрузка изменений из remote во временные ветки отслеживания refs/remotes/) и `git merge` (слияние удаленной ветки в текущую локальную ветку)',
      'Она инициирует SSH-туннель для постоянного отслеживания изменений на сервере',
      'Она удаляет все неотслеживаемые файлы локально и приводит локальный репозиторий в полное соответствие с сервером'
    ],
    correctAnswer: 1,
    explanation: 'Команда `git pull` является сокращением для двух последовательных действий: `git fetch` (запрос и скачивание новых объектов и ссылок из удаленного репозитория) и `git merge FETCH_HEAD` (слияние полученной ветки в текущую локальную ветку). Вместо merge можно настроить поведение rebase через `git pull --rebase`.',
  },
  {
    id: 'git-22',
    category: 'git',
    question: 'Почему в больших командах часто рекомендуется использовать `git pull --rebase` вместо стандартного `git pull`?',
    options: [
      'git pull --rebase выполняется быстрее, так как не скачивает объекты коммитов, а только файлы',
      'git pull --rebase гарантирует автоматическое разрешение любых возникающих конфликтов слияния без участия разработчика',
      'git pull --rebase предотвращает засорение истории коммитов лишними автоматическими мерж-коммитами ("Merge branch ... of ...") и выстраивает локальные коммиты поверх свежих изменений с сервера, сохраняя историю линейной',
      'git pull --rebase отключает запуск pre-commit хуков, ускоряя интеграцию'
    ],
    correctAnswer: 2,
    explanation: 'При обычном `git pull`, если у вас есть локальные коммиты и на сервере появились новые, Git создаст мерж-коммит для их объединения. При `git pull --rebase` Git забирает изменения с сервера (`fetch`), временно откладывает ваши локальные коммиты, обновляет локальную ветку до состояния удаленной, а затем применяет ваши коммиты поверх. Это предотвращает генерацию "пустых" мерж-коммитов слияния.',
  },
  {
    id: 'git-23',
    category: 'git',
    question: 'В чем заключается разница в безопасности между командами `git push --force` и `git push --force-with-lease`?',
    options: [
      '--force-with-lease требует ввода пароля GPG-ключа перед отправкой',
      '--force-with-lease проверяет, не появились ли на удаленном сервере новые коммиты, которых нет в нашей локальной копии удаленной ветки (refs/remotes/...). Если на сервере есть чужие новые коммиты, push будет заблокирован',
      '--force работает медленнее, так как полностью пересоздает репозиторий на сервере, а --force-with-lease отправляет только дельты',
      '--force-with-lease работает только при наличии настроенного SSL-сертификата в репозитории'
    ],
    correctAnswer: 1,
    explanation: '`git push --force` слепо перезаписывает ветку на сервере локальной версией, стирая любые коммиты коллег, отправленные после вашего последнего fetch. Флаг `--force-with-lease` проверяет, совпадает ли хэш удаленной ветки на сервере с локальной веткой отслеживания (т.е. что никто не отправлял коммиты с момента вашего последнего `git fetch`). Если изменения были внесены другими разработчиками, команда завершится ошибкой.',
  },
  {
    id: 'git-24',
    category: 'git',
    question: 'Как установить связь локальной ветки с удаленной веткой на сервере (set upstream) при первой отправке коммитов?',
    options: [
      'git push -u origin <branch-name> (или --set-upstream)',
      'git push --link-to-remote <branch-name>',
      'git remote set-branch origin <branch-name>',
      'git branch --connect origin/<branch-name>'
    ],
    correctAnswer: 0,
    explanation: 'Флаг `-u` или `--set-upstream` в команде `git push` связывает текущую локальную ветку с удаленной веткой на сервере. В дальнейшем для этой ветки можно будет выполнять команды `git pull` и `git push` без указания имени удаленного репозитория и ветки.',
  },
  {
    id: 'git-25',
    category: 'git',
    question: 'Что означают маркеры конфликта `<<<<<<< HEAD`, `=======` и `>>>>>>> <branch-name>` в файле при возникновении конфликта слияния?',
    options: [
      'Это служебные символы для шифрования файлов при передаче по SSH',
      'Между <<<<<<< HEAD и ======= находятся изменения из вашей текущей локальной ветки, а между ======= и >>>>>>> находятся изменения из вливаемой ветки',
      'Это разметка синтаксического анализатора Markdown, добавленная GitHub автоматически',
      'Между <<<<<<< HEAD и ======= находятся удаленные строки, а ниже — новые строки, добавленные компилятором'
    ],
    correctAnswer: 1,
    explanation: 'При конфликте слияния Git размечает конфликтующие строки в файле: `<<<<<<< HEAD` указывает на начало блока из текущей активной ветки (HEAD); `=======` является разделителем; `>>>>>>> <branch-name>` указывает на окончание блока изменений из сливаемой ветки.',
  },
  {
    id: 'git-26',
    category: 'git',
    question: 'Как заставить Git игнорировать файл, который уже был закоммичен и находится под версионным контролем, добавив его в `.gitignore`?',
    options: [
      'Достаточно просто добавить путь к файлу в .gitignore, и Git сразу перестанет его отслеживать',
      'Нужно удалить файл физически с диска, закоммитить это удаление, а затем создать его заново',
      'Нужно сначала удалить файл из индекса (но сохранить на диске) с помощью команды `git rm --cached <file>`, добавить его в `.gitignore` и закоммитить изменения',
      'Использовать команду git config core.ignore <file>'
    ],
    correctAnswer: 2,
    explanation: 'Если файл уже отслеживается Git, запись в `.gitignore` не прекратит его отслеживание. Чтобы исправить это, нужно сначала удалить его из индекса (сохранив локально в рабочей директории) командой `git rm --cached <file>`, затем добавить путь в `.gitignore` и закоммитить.',
  },
  {
    id: 'git-27',
    category: 'git',
    question: 'Где физически хранятся Git hooks (хуки) в локальном репозитории и как временно обойти их выполнение (например, pre-commit)?',
    options: [
      'В папке .git/hooks/; обойти выполнение можно с помощью флага `--no-verify` при коммите',
      'В файле .gitignore; обойти выполнение можно, закомментировав соответствующие строки',
      'В глобальной конфигурации пользователя; обойти выполнение можно флагом --force-push',
      'В базе данных коммитов (объект commit); обойти выполнение невозможно'
    ],
    correctAnswer: 0,
    explanation: 'Хуки Git хранятся локально в директории `.git/hooks/` в виде исполняемых скриптов. Так как эта папка не коммитится в общий репозиторий, хуки не передаются при клонировании по умолчанию. Чтобы пропустить выполнение хуков (например, линтеров на pre-commit или pre-push), используется флаг `--no-verify` (или `-n` для некоторых команд).',
  },
  {
    id: 'git-28',
    category: 'git',
    question: 'В чем основное отличие методологии Trunk-Based Development от классического Git Flow?',
    options: [
      'Trunk-Based Development использует исключительно ветки "hotfix", запрещая создание веток фич',
      'Trunk-Based Development предполагает, что разработчики работают в одной общей ветке (trunk/main) или очень короткоживущих ветках и сливают свои изменения в main несколько раз в день, избегая долгоживущих feature-веток',
      'Git Flow не поддерживает использование pull request-ов, в отличие от Trunk-Based Development',
      'Trunk-Based Development требует обязательного ручного релиза на каждый коммит'
    ],
    correctAnswer: 1,
    explanation: 'Git Flow основан на долгоживущих ветках `develop`, `master`, а также ветках фич, релизов и хотфиксов, что часто ведет к сложным слияниям. Trunk-Based Development минимизирует время жизни веток: все разработчики коммитят в одну основную ветку (`trunk` или `main`) напрямую или через мелкие PR, которые сливаются ежедневно, что отлично подходит для CI/CD.',
  },
  {
    id: 'git-29',
    category: 'git',
    question: 'Какие типы коммитов согласно спецификации Conventional Commits указывают на изменения, которые будут зафиксированы в Semantic Versioning как MINOR и PATCH релизы соответственно?',
    options: [
      'minor и patch соответственно',
      'docs (MINOR) и test (PATCH)',
      'feat (MINOR) и fix (PATCH) — при отсутствии знака "!" или "BREAKING CHANGE" в теле сообщения',
      'refactor (MINOR) и chore (PATCH)'
    ],
    correctAnswer: 2,
    explanation: 'По спецификации Conventional Commits: коммит типа `feat` добавляет новую функциональность и соответствует увеличению MINOR версии. Коммит типа `fix` исправляет баг и соответствует увеличению PATCH версии. Мажорная версия (MAJOR) увеличивается при наличии текста `BREAKING CHANGE:` в футере или восклицательного знака после типа (например, `feat!: ...`).',
  },
  {
    id: 'git-30',
    category: 'git',
    question: 'В чем ключевое отличие между легковесными (lightweight) и аннотированными (annotated) тегами в Git?',
    options: [
      'Легковесные теги создаются локально, а аннотированные автоматически отправляются на удаленный сервер без флага --tags',
      'Легковесный тег — это просто указатель на коммит (как ветка, но не двигающаяся), а аннотированный тег создается как полноценный объект в базе данных Git, содержащий имя автора, email, дату, сообщение и опционально GPG-подпись',
      'Аннотированные теги можно ставить только на коммиты слияния (merge commits)',
      'Легковесные теги автоматически удаляются сборщиком мусора через 30 дней'
    ],
    correctAnswer: 1,
    explanation: 'Легковесный тег — это обычная неизменяемая ссылка на коммит (создается командой `git tag <tag-name>`). Аннотированный тег (`git tag -a <tag-name> -m "сообщение"`) — это самостоятельный объект Git, хранящий метаданные (кто создал, когда, описание) и имеющий собственный SHA-1 хэш. На практике для релизов рекомендуется использовать аннотированные теги.',
  },
  {
    id: 'git-31',
    category: 'git',
    question: 'Как использовать команду `git bisect` для автоматического поиска коммита, который сломал сборку, если у вас есть скрипт тестирования `npm run test`?',
    options: [
      'Запустить `git bisect start`, пометить текущий коммит плохим (`git bisect bad`), заведомо рабочий хорошим (`git bisect good <hash>`), а затем выполнить `git bisect run npm run test`',
      'Запустить `git bisect npm run test` — Git сам просканирует всю историю',
      'Команда `git bisect` не поддерживает автоматический запуск скриптов и требует ручного переключения',
      'Выполнить `git bisect --auto-run "npm run test"`'
    ],
    correctAnswer: 0,
    explanation: '`git bisect` выполняет бинарный поиск по истории коммитов. Вы инициализируете процесс (`git bisect start`), указываете плохой коммит (обычно текущий: `git bisect bad`) и заведомо хороший коммит (`git bisect good <hash>`). После этого команда `git bisect run <command>` будет автоматически переключать коммиты и запускать скрипт, определяя результат по коду выхода (0 — хорошо, 1-127 — плохо), пока не найдет первый "сломанный" коммит.',
  },
  {
    id: 'git-32',
    category: 'git',
    question: 'Какая опция команды `git blame` позволяет игнорировать изменения пробелов и переносов строк при поиске автора изменения конкретной строки кода?',
    options: [
      'git blame --ignore-all',
      'git blame -w',
      'git blame --skip-whitespace',
      'git blame -f'
    ],
    correctAnswer: 1,
    explanation: 'Флаг `-w` в команде `git blame` заставляет Git игнорировать изменения пробелов и форматирования при анализе файла. Это помогает найти реального автора бизнес-логики строки, если после него кто-то просто отформатировал файл с помощью Prettier или другого линтера.',
  },
  {
    id: 'git-33',
    category: 'git',
    question: 'Для чего используется команда `git shortlog`?',
    options: [
      'Для вывода лога только за последние 24 часа в сокращенном формате',
      'Для группировки коммитов по авторам, отображая имя автора и список сообщений коммитов либо их количество (с флагом `-s`)',
      'Для генерации Changelog-файла в формате Markdown',
      'Для просмотра истории изменений внутри одного конкретного файла'
    ],
    correctAnswer: 1,
    explanation: '`git shortlog` предназначена для суммирования вывода `git log`. Она группирует коммиты по авторам. С флагом `-s` (summary) она показывает только общее число коммитов для каждого автора, а с флагом `-n` сортирует авторов по количеству внесенных коммитов, что полезно для сбора статистики перед релизом.',
  },
  {
    id: 'git-34',
    category: 'git',
    question: 'Что такое Shallow clone (`git clone --depth <N>`) и каково его основное ограничение?',
    options: [
      'Это клонирование только файлов без структуры папок; ограничение — нельзя запускать проект локально',
      'Это создание клона, содержащего историю только за последние N дней; ограничение — невозможно переключать ветки',
      'Это клонирование репозитория с усеченной историей, содержащей только N последних коммитов; ограничение — при попытке сделать push в репозиторий без полной истории могут возникнуть проблемы слияния и отклонение сервером (требуется углубление истории)',
      'Это клонирование только метаданных репозитория без скачивания самих файлов проекта'
    ],
    correctAnswer: 2,
    explanation: 'Shallow clone (`--depth N`) скачивает репозиторий с обрезанной историей коммитов (глубиной N коммитов от HEAD). Это экономит трафик и дисковое пространство на CI-серверах. Главное ограничение — усеченная история затрудняет вычисление баз слияния для старых веток, из-за чего push/pull могут завершаться ошибками, пока вы не стянете недостающую историю через `git fetch --unshallow`.',
  },
  {
    id: 'git-35',
    category: 'git',
    question: 'Что такое Sparse checkout в Git и когда его целесообразно использовать?',
    options: [
      'Это проверка базы данных .git на наличие битых файлов и dangling-объектов',
      'Это механизм, позволяющий загружать/извлекать в рабочую директорию только определенный набор папок и файлов из репозитория, а не весь проект целиком; полезно в сверхбольших монорепозиториях',
      'Это режим работы Git, при котором изменения коммитятся сразу на удаленный сервер в обход локальной папки',
      'Это инструмент для автоматического удаления неиспользуемых веток на основе активности в Jira/GitHub'
    ],
    correctAnswer: 1,
    explanation: 'Sparse checkout (разреженное извлечение) позволяет настроить Git так, чтобы в вашей рабочей директории находились только файлы из определенных папок крупного репозитория (например, только папка одного микросервиса в гигантском монорепозитории). Настраивается с помощью команд `git sparse-checkout init` и `git sparse-checkout set <dir>`.',
  },
  {
    id: 'git-36',
    category: 'git',
    question: 'Что представляет собой Git Submodule (подмодуль) под капотом родительского репозитория?',
    options: [
      'Простую копию папки стороннего проекта, файлы которой коммитятся напрямую в родительский репозиторий',
      'Специальную запись в базе данных, содержащую жесткую ссылку на коммит из другого репозитория. В родительском проекте подмодуль представлен файлом `.gitmodules` и коммитом с точным SHA-1, указывающим на состояние внешней папки',
      'Автоматический симлинк операционной системы, указывающий на внешнюю директорию на диске',
      'Скрытый XML-файл с конфигурацией всех внешних NPM-зависимостей'
    ],
    correctAnswer: 1,
    explanation: 'Git Submodule позволяет встраивать один репозиторий внутрь другого как поддиректорию. Родительский репозиторий не отслеживает внутренние файлы подмодуля. Вместо этого он хранит файл конфигурации `.gitmodules` и специальный тип записи в индексе (gitlink), содержащий точный SHA-1 коммита внешнего репозитория, который должен быть извлечен в этой папке.',
  },
  {
    id: 'git-37',
    category: 'git',
    question: 'Для чего используется команда `git worktree`?',
    options: [
      'Для создания визуального интерактивного дерева изменений в браузере',
      'Для одновременной работы со всеми ветками репозитория в рамках одной папки проекта',
      'Для создания дополнительных рабочих директорий (связанных с тем же локальным репозиторием .git), что позволяет держать извлеченными разные ветки в разных папках на диске и работать в них параллельно',
      'Для архивации неиспользуемых веток сжатием их в формат .tar.gz'
    ],
    correctAnswer: 2,
    explanation: '`git worktree` решает проблему, когда вам нужно срочно переключиться на другую ветку (например, для починки бага), не прерывая работу в текущей ветке и не делая `stash`/`commit`. Команда `git worktree add <path> <branch>` создает новую физическую папку на диске, привязанную к той же базе `.git`, где развернута указанная ветка. Вы можете параллельно работать и запускать тесты в обеих папках.',
  },
  {
    id: 'git-38',
    category: 'git',
    question: 'Какое решение является наиболее эффективным для масштабирования производительности Git в гигантских монорепозиториях (сотни гигабайт кода)?',
    options: [
      'Отключение отслеживания истории через переинициализацию репозитория каждые две недели',
      'Использование Git LFS для бинарных ассетов, включение файлового монитора (FSMonitor) для ускорения git status, настройка sparse-checkout и использование виртуальной файловой системы (VFS for Git / Scalar)',
      'Разбиение монорепозитория на физические ZIP-архивы, хранящиеся на сервере',
      'Увеличение лимита оперативной памяти ОС для процесса git.exe с помощью переменной окружения GIT_MAX_RAM'
    ],
    correctAnswer: 1,
    explanation: 'Для работы со сверхкрупными монорепозиториями применяются: Git LFS (для выноса тяжелых медиафайлов), встроенный FSMonitor (`git config core.fsmonitor true`) для мгновенного отклика `git status`, sparse-checkout для сужения рабочей области, а также технологии Scalar (разработанные Microsoft) для оптимизации внутренних индексов Git.',
  },
  {
    id: 'git-39',
    category: 'git',
    question: 'Что происходит при включении политики Branch Protection (защита веток) для ветки `main` на GitHub/GitLab?',
    options: [
      'Доступ к ветке шифруется уникальным паролем, который отправляется разработчикам по SMS',
      'Ветка удаляется из локальных копий всех разработчиков, становясь доступной только в CI/CD',
      'Блокируется возможность force push-а, запрещается прямое удаление ветки, а также могут требоваться прохождение CI/CD тестов (status checks) и наличие аппрувов от код-ревьюеров перед слиянием PR',
      'Ветка main автоматически синхронизируется с ветками develop во всех форках проекта раз в час'
    ],
    correctAnswer: 2,
    explanation: 'Branch Protection (или Protected Branches) — это настройка безопасности на хостингах Git (GitHub, GitLab и др.). Она предотвращает случайное или намеренное удаление и перезапись истории (`force push`), а также позволяет настроить правила качества: обязательное прохождение тестов в CI, отсутствие конфликтов, а также минимальное число одобрений (approvals) от коллег в Pull/Merge Request.',
  },
  {
    id: 'git-40',
    category: 'git',
    question: 'Зачем разработчики настраивают подписание коммитов с помощью GPG или SSH ключей?',
    options: [
      'Для шифрования исходного кода коммита, чтобы никто посторонний не смог прочитать файлы на сервере',
      'Для подтверждения авторства коммита. Это гарантирует, что коммит был создан именно владельцем ключа, предотвращая подделку имени автора и email (который в Git можно легко указать любым в git config)',
      'Для автоматического ускорения сетевой передачи данных при git push',
      'Для обхода обязательного прохождения тестов в CI/CD пайплайнах'
    ],
    correctAnswer: 1,
    explanation: 'В Git любой человек при создании коммита может прописать в конфигурации абсолютно любое имя и email (например, создателя Git Линуса Торвальдса). Чтобы гарантировать, что коммит действительно сделан конкретным разработчиком, используется криптографическая подпись (GPG/SSH). Платформы вроде GitHub проверяют эту подпись по публичному ключу пользователя и вешают зеленую плашку "Verified" рядом с коммитом.',
  },
  {
    id: 'git-41',
    category: 'git',
    question: 'В чем разница между стратегиями слияния Pull Request: "Rebase and Merge" и "Squash and Merge" на GitHub?',
    options: [
      'Rebase and Merge сливает все коммиты в один, а Squash and Merge сохраняет всю цепочку коммитов без изменений',
      'Rebase and Merge переносит всю цепочку коммитов из PR по очереди поверх целевой ветки (без создания merge-коммита), а Squash and Merge объединяет все коммиты из PR в один-единственный коммит и добавляет его в целевую ветку',
      'Squash and Merge стирает код и оставляет только текстовые логи, а Rebase and Merge делает обратное',
      'Обе стратегии идентичны, но Squash and Merge используется только для веток релиза'
    ],
    correctAnswer: 1,
    explanation: 'При "Rebase and Merge" каждый коммит из PR переносится на целевую ветку отдельно, сохраняя подробную историю разработки (но коммиты получают новые SHA хэши). При "Squash and Merge" все коммиты ветки PR объединяются в один аккуратный коммит, что делает историю целевой ветки (например, `main`) очень чистой и высокоуровневой.',
  },
  {
    id: 'git-42',
    category: 'git',
    question: 'В каком приоритете Git применяет конфигурационные настройки из файлов `system`, `global` и `local` (от наивысшего приоритета к наинизшему)?',
    options: [
      'local -> global -> system',
      'system -> global -> local',
      'global -> local -> system',
      'local -> system -> global'
    ],
    correctAnswer: 0,
    explanation: 'Git ищет настройки по цепочке от частного к общему: сначала проверяется локальный файл настроек конкретного репозитория (`.git/config` — local), затем глобальные настройки текущего пользователя (`~/.gitconfig` — global), и в конце — системные настройки для всех пользователей ОС (`/etc/gitconfig` — system). Локальные настройки имеют наивысший приоритет и переопределяют глобальные.',
  },
  {
    id: 'git-43',
    category: 'git',
    question: 'Что означает концепция "атомарных коммитов" (Atomic Commits) в методологии работы с Git?',
    options: [
      'Каждый коммит должен содержать изменения ровно в одном файле репозитория',
      'Каждый коммит должен содержать ровно одну минимально необходимую, логически завершенную задачу (или часть задачи), которая не ломает сборку проекта и может быть легко откачена без побочных эффектов',
      'Коммиты должны создаваться автоматически каждые 15 минут с помощью планировщика задач',
      'Все изменения в проекте должны упаковываться в один гигантский коммит перед релизом'
    ],
    correctAnswer: 1,
    explanation: 'Атомарный коммит решает одну конкретную задачу (например: добавление новой кнопки, исправление конкретной уязвимости, рефакторинг одной функции). Код внутри коммита должен быть стабильным (проект компилируется и тесты проходят). Такие коммиты легко ревьюить, переносить через `cherry-pick` и откатывать назад через `git revert` в случае багов.',
  },
  {
    id: 'git-44',
    category: 'git',
    question: 'Какие четыре типа объектов составляют основу объектной базы данных (object database) Git?',
    options: [
      'blob, tree, commit, tag',
      'file, folder, branch, remote',
      'text, binary, pointer, reference',
      'diff, patch, log, status'
    ],
    correctAnswer: 0,
    explanation: 'Внутреннее устройство Git опирается на 4 типа объектов: `blob` (содержимое файлов без метаданных), `tree` (каталоги, связывающие имена файлов со ссылками на blobs или другие trees), `commit` (указатель на корневое дерево, родителей, дату и автора) и `tag` (аннотированная ссылка на конкретный коммит).',
  },
  {
    id: 'git-45',
    category: 'git',
    question: 'Как вычисляется хэш SHA-1 (или SHA-256) коммита в Git и почему он гарантирует целостность истории?',
    options: [
      'Он зависит только от текста сообщения коммита и текущего времени системы',
      'Он рассчитывается на основе содержимого объекта коммита: хэша корневого дерева файлов, хэшей родительских коммитов, автора, даты и сообщения коммита. Изменение любого файла или родителя полностью изменит хэш коммита и всех последующих коммитов',
      'Он генерируется случайным образом при каждом запуске команды git commit',
      'Он выдается удаленным сервером GitHub при регистрации коммита в базе данных'
    ],
    correctAnswer: 1,
    explanation: 'SHA-1 коммита — это хэш-сумма от содержимого объекта коммита, включая хэш структуры файлов проекта (`tree`) и хэши родителей (`parent`). Если изменить хотя бы один символ в старом файле, изменится хэш его blob, затем хэш содержащего его tree, хэш коммита, а следом изменятся хэши всех последующих коммитов в цепочке. Это делает историю Git криптографически защищенной от незаметных изменений.',
  },
  {
    id: 'git-46',
    category: 'git',
    question: 'Для чего используются команды `git gc` и `git prune` в Git?',
    options: [
      'Для удаления неиспользуемых локальных веток, которые уже слиты в main на сервере',
      'Для удаления временных папок сборки (например, node_modules или dist)',
      'Для очистки и оптимизации локального репозитория: сборки мусора, упаковки объектов в pack-файлы и удаления висячих (dangling/orphan) объектов, не связанных ни с какими ветками',
      'Для автоматического исправления синтаксических ошибок в кодовой базе'
    ],
    correctAnswer: 2,
    explanation: 'С течением времени в репозитории накапливаются "висячие" объекты (например, коммиты после rebase или сброшенные ветки). Команда `git gc` (garbage collection) оптимизирует базу данных, сжимая объекты в pack-файлы. Команда `git prune` вызывается внутри gc и физически удаляет неиспользуемые объекты, на которые никто не ссылается в refs или reflog.',
  },
  {
    id: 'git-47',
    category: 'git',
    question: 'Какая команда удалит локальные ветки отслеживания удаленного репозитория (remote-tracking branches), которые уже были удалены на самом сервере?',
    options: [
      'git fetch --prune (или git remote prune origin)',
      'git remote remove --all',
      'git push origin --delete --local',
      'git branch -d -r'
    ],
    correctAnswer: 0,
    explanation: 'Когда коллеги удаляют ветки на сервере, ваши локальные ветки отслеживания (вида `origin/feature-name`) остаются висеть в выводе `git branch -r`. Чтобы синхронизировать список и удалить "осиротевшие" ссылки на удаленные ветки, нужно запустить `git fetch --prune` или `git remote prune origin`.',
  },
  {
    id: 'git-48',
    category: 'git',
    question: 'Какая команда позволяет объединить последние 3 коммита в один перед отправкой изменений в Pull Request?',
    options: [
      'git reset --hard HEAD~3 && git commit -m "Merged"',
      'git merge --squash HEAD~3',
      'git rebase -i HEAD~3 (с последующей заменой pick на squash/fixup для 2-го и 3-го коммитов)',
      'git revert HEAD~3..HEAD'
    ],
    correctAnswer: 2,
    explanation: 'Для объединения произвольного количества коммитов используется интерактивный ребейз: `git rebase -i HEAD~3`. В открывшемся текстовом файле для первого коммита оставляется инструкция `pick`, а для последующих двух она меняется на `squash` (или `fixup`). При сохранении Git объединит изменения этих трех коммитов в один.',
  },
  {
    id: 'git-49',
    category: 'git',
    question: 'Как работает технология Git LFS (Large File Storage) при коммите тяжелых бинарных файлов (например, видео или 3D-моделей)?',
    options: [
      'Она автоматически сжимает файлы в архивы с паролем перед отправкой на сервер',
      'Она заменяет тяжелые файлы в самом репозитории Git на легкие текстовые указатели (содержащие SHA-256 хэш и размер), а сами оригинальные файлы загружает на специальное LFS-хранилище по HTTPS',
      'Она делит файлы на мелкие текстовые чанки и коммитит их как обычные строки кода',
      'Она сохраняет файлы во временной памяти видеокарты разработчика'
    ],
    correctAnswer: 1,
    explanation: 'Поскольку Git устроен так, что хранит всю историю изменений каждого файла, добавление тяжелых бинарных файлов быстро раздувает размер папки `.git` до гигабайт. Git LFS решает эту проблему: в истории Git сохраняется только крошечный текстовый файл-указатель (pointer) с метаданными, а сам тяжелый файл скачивается со специализированного LFS-сервера только при извлечении (checkout) конкретного коммита.',
  },
  {
    id: 'git-50',
    category: 'git',
    question: 'Для чего предназначена опция `rerere` (reuse recorded resolution) в Git и как ее активировать?',
    options: [
      'Для циклического повторения анимации коммитов в терминале',
      'Для автоматического отката коммитов, если тесты в CI/CD завершились неудачно',
      'Для записи того, как вы разрешили конфликт слияния в файле, чтобы при возникновении точно такого же конфликта в будущем Git разрешил его автоматически; активируется через `git config --global rerere.enabled true`',
      'Для восстановления удаленных тегов релиза'
    ],
    correctAnswer: 2,
    explanation: 'Опция `rerere` расшифровывается как "reuse recorded resolution" (повторное использование записанного разрешения конфликтов). Если вы часто делаете rebase длинной ветки или сливаете ее туда-обратно, вам может приходиться разрешать одни и те же конфликты многократно. При включенном `rerere.enabled` Git запоминает ваши исправления конфликта и автоматически применяет их, экономя время разработчика.'
  }
];
